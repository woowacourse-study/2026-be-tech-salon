name: Build README

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".automation/templates/readme_header.md"
      - ".automation/presentations.yml"

permissions:
  contents: write

concurrency:
  group: repo-mutation
  cancel-in-progress: false

jobs:
  build:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y poppler-utils

      - name: Install Python deps
        run: pip install pyyaml

      - name: Generate thumbnails
        run: python .automation/scripts/generate_thumbnails.py

      - name: Regenerate README
        run: python .automation/scripts/generate_readme.py

      - name: Commit changes
        run: |
          if [[ -z "$(git status --porcelain)" ]]; then
            echo "nothing to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .automation/presentations.yml README.md images/
          git commit -m "chore: auto-update thumbnails and README"
          git push
